"""lint — not implemented in Session 1 (CLAUDE.md §7).

Scaffolded per the §4.2 repo layout. Implementing this before the git-ingest
shell passes its audit would violate the staged-ingest guardrail (§6.1).
"""

from __future__ import annotations


def lint(*args, **kwargs):  # pragma: no cover - placeholder
    raise NotImplementedError("lint is scheduled for a later phase (see CLAUDE.md §7).")
